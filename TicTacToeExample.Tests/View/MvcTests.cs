using TicTacToeExample.View;
using Xunit;

namespace TicTacToeExample.Test.View
{
    public class MvcTests
    {
        private Mvc view;

        [WpfFact]
        public void CreateTest()
        {
            // Couldn't run test
            // Caused by StaticResource MaterialDesignRaisedDarkButton in Buttons
            /*view = new Mvc();

            Assert.NotNull(view);*/
        }
    }
}